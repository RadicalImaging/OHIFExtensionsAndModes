{
  "name": "root",
  "private": true,
  "workspaces": [
    "config-mode",
    "hp-extension",
    "hp-mode",
    "volume-mode",
    "findings-mode"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "publishAll": "yarn build && lerna run publishItem --stream",
    "zzz": "End item"
  },
  "devDependencies": {
    "lerna": "^5.4.2"
  }
}
